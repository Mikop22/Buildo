Desired product/device to assemble: {task_description}

Parts to be used (in JSON format with categories, subcategories, and product details):
{parts_json}

Example output format (return ONLY a JSON array of strings, where each string is one clear instruction step):
["Step 1: Prepare all components and identify each part from the parts list", "Step 2: Place the ESP32 breakout board on the breadboard, ensuring proper orientation", "Step 3: Connect the soil moisture sensor VCC pin to 3.3V power rail, GND to ground rail, and signal pin to GPIO pin 5 on the ESP32", "Step 4: Connect the relay module VCC to 5V, GND to ground, and control pin to GPIO pin 2 on the ESP32", "Step 5: Connect the OLED display VCC to 3.3V, GND to ground, SDA to GPIO 21, and SCL to GPIO 22 on the ESP32", "Step 6: Connect the power supply positive terminal to the 5V rail and negative terminal to the ground rail, ensuring proper voltage ratings"]

Make each step clear, actionable, and reference specific parts from the parts list. Do not include any text outside the JSON array.
